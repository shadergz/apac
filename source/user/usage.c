#include <user/usage.h>

const char *g_usagemsg = {
  "usage of (apac) [-/-- options --in=$INPUT --out=$OUTPUT ]:\n"
  "	-h\\--help:      Display user help table (it's exactly what y're "
  "seeing!)\n"
};
